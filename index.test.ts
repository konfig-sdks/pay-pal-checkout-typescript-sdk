import { PayPalCheckout } from "./index";

describe("pay-pal-checkout-typescript-sdk", () => {
    it("initialize client", async () => {
        const paypalcheckout = new PayPalCheckout({
        });
    });
});
